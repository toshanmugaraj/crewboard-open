// MarkerPopup is handled inline via Leaflet's bindPopup in MapBoard.jsx
// This file exists to satisfy the import — the actual popup logic is in MapBoard
export default function MarkerPopup() { return null }
